{
  "name": "cham-cong-tablet",
  "version": "1.0.0",
  "description": "APK chấm công khuôn mặt cho tablet",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build": "cd android && gradlew.bat assembleDebug",
    "apk": "npm run sync && npm run build"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
